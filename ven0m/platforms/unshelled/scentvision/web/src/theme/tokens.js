/**
 * Voidweave — canonical tokens: platforms/unshelled/design-language/voidweave/tokens.json
 * Edit the JSON; this file only re-exports for the app.
 */
import pack from "../../../../design-language/voidweave/tokens.json";

export const T = pack.palette;
export const F = pack.typography;
