# Canonical remote — venom-opencode

**Upstream OpenCode:** https://github.com/anomalyco/opencode (`dev`)

**Unshelled fork (VENOM integration / experiments):** https://github.com/Unshelled/venom-opencode  
- Fork of `anomalyco/opencode` · default branch **`dev`**

**Later:** org/domain move (e.g. unshelled.ai GitHub) = transfer or new `origin`; no extra fork required unless you want a **non-fork** repo for product branding.

**This machine:** clone the fork when doing source-level work; keep `venom-mine` origin separate unless you deliberately merge workflows.

---

*2026-03-27 — recorded from user.*
